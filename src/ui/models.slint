export struct WallpaperItem {
  id: string,
  title: string,
  details: string,
  image_path: string,
  info_url: string,
  preview_path: string,
  thumbnail: image,
  thumbnail_ready: bool,
  favorite: bool,
}
